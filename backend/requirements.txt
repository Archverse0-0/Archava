livekit-agents[tavus]==1.7.1
livekit-plugins-google==1.7.1
livekit-plugins-noise-cancellation==0.3.0
livekit-plugins-tavus==1.7.1
google-genai==2.20.0
requests
python-dotenv
pydantic-ai-slim[openai,mcp]
tzdata
flask==3.1.3
flask-cors

# Test dependencies. ``pytest-asyncio`` is required: the tool tests are
# ``async def`` and fail to run ("async def functions are not natively
# supported") without it, which is what broke the backend CI job.
# pytest is pinned to >=9.0.3 because 8.x carries PYSEC-2026-1845 (an
# unmaintained-import RCE reachable from a crafted config file); the pinned
# version keeps ``pip-audit -r requirements.txt`` clean in CI.
pytest==9.0.3
pytest-asyncio==1.4.0
